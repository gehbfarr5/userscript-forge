# 公开脚本目录

这里仅记录公开脚本仓库的发现信息，不保存各脚本的 Git 提交指针，也不是脚本发布状态的第二真相源。

当前目录中的 `projects.json` 只记录基础设施检查脚本的公开地址和粗粒度阶段状态。

`capabilities.json` 是脱敏能力摘要，不替代私密 `result.json`。只有结构化 evidence 或明确的仓库状态才能把摘要写成 `PASS`；`BLOCKED` 和 `NOT_RUN` 必须保留，供 Agent 判断发布门禁。
